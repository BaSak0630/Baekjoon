#include<stdio.h>

main()
{
	int y;
 
	scanf("%d",&y); //오늘도 여기서 &를 안붙여서 런타임에러 이니쉬에이트드가 뜸
	
	
	if(y % 4 == 0 && y % 100 != 0 || y % 400 == 0)//연도가 4로나눈 나머지가 0이고 100으로 나눈 나머지가 영일때
	{
			printf("1");
	}

	else
	{
		printf("0");
	}


}
